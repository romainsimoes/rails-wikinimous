<div><%= link_to 'CREATE NEW ARTICLE', new_article_path %></div></div>

<div class='welcome'><h1>WELCOME TO WIKINIMOUS</h1></div>

<% @articles.each do |article| %>
  <div class='padded text-center'>
    <h2><%= link_to article.title, article_path(article) %></h2>
    <p><%= raw @markdown.render("#{article.content}") %></p>
  <div>
<% end %>
